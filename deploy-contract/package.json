{
  "name": "eweb3-contract-deploy",
  "version": "1.0.0",
  "description": "Deploy TransactionRegistry contract",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "deploy": "hardhat run scripts/deploy.js --network baseSepolia"
  },
  "devDependencies": {
    "hardhat": "^2.22.10",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5"
  }
}